[build]
builder = "NIXPACKS"
buildCommand = "cd backend && python3 -m pip install -e ."

[deploy]
startCommand = "cd backend && python3 -m alembic upgrade head && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
